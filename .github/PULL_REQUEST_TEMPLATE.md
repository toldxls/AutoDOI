## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `./build.sh` was run after editing `citations.js`, `parsers.js` or `sentencecase.js` (CI fails otherwise)
- [ ] `npm test` passes
- [ ] A citation rule change comes with a test in `tests/` and, if it is a journal style, a note of the source (author guidelines URL)
- [ ] `CHANGELOG.md` has an entry under *Unreleased* if users would notice the change
